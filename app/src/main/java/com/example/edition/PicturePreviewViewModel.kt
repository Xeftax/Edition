package org.linphone.activities.main.preview

import android.widget.TextView
import androidx.lifecycle.ViewModel

class PicturePreviewViewModel : ViewModel() {
    var textDataList: List<List<Any>> = ArrayList()

    fun addTextData(textView: TextView) {
        var textData: ArrayList<Any> = ArrayList()
    }
}
